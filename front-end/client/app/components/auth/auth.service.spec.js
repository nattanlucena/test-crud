import AuthModule from './auth'
import AuthService from './auth.service';

describe('Auth', () => {
  let $rootScope, AuthService;

  describe('Module', () => {
    // top-level specs: i.e., routes, injection, naming
  });
});
