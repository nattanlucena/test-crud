import template from './postForm.html';
import controller from './postForm.controller';
import './postForm.scss';

let postFormComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default postFormComponent;
