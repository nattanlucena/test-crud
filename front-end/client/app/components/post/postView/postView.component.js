import template from './postView.html';
import controller from './postView.controller';
import './postView.scss';

let postViewComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default postViewComponent;
